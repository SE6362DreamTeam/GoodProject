# FOLLOWING IMPLICIT INVOCATION ARCH: could use boolean flags that are checked periodically
# Once the flag is true, method begins doing its task (implicitly invoked)

# Class KWIC
# method readInput:

# method circularshift:

# method alphabetize:

# method storeOutput:

# main method
    # calls readInput, passes input file to it
    # calls storeOutput once a flag (lineProcessed is set to true)